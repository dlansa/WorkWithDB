package com.epam.library.service;

import com.epam.library.domain.DBconnector;

import java.sql.*;

public class JournalCreator {


    public static void createTable() {

            /*DBconnector connector = new DBconnector();
            Statement statement = null;
            ResultSet resultSet = null;
            //String query = "CREATE TABLE `corporatebooksdb`.`employee_book` (`id` INT NOT NULL AUTO_INCREMENT," +
            //        "`id_employee` INT NOT NULL, `id_book` INT NOT NULL, RIMARY KEY (`id`));";

            try {
                statement = connector.getConnection().createStatement();
                resultSet = statement.executeQuery(query);
            } catch (SQLException e) {
                e.printStackTrace();
            }
    }
}*/
    }
}
